name: image
version: 0.1.0

deps:
  - git@github.com:panda-io/micro-panda-gfx.git@latest

targets:
  main:
    entry: main
    src: src/
    test: test/
    type: bin
    flags: [RELEASE, HOSTED]
    output: bin/main
    cc:
      bin: gcc
      flags: [-g, -O2, -Wall]
